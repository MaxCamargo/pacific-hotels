import { Funnel_Sans, Ephesis } from 'next/font/google'
 
export const funnel_sans = Funnel_Sans({
  subsets: ['latin'],
  display: 'swap',
})

export const ephesis = Ephesis({
  weight: ['400'],
  subsets: ['latin'],
  display: 'swap',
})